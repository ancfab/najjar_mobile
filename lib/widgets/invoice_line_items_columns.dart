/// Flex weights for the Invoice line-items table's four columns, shared by
/// [InvoiceLineItemsHeader] and [InvoiceLineItemRow] so header and row cells
/// always line up.
const int kInvoiceItemDetailsColumnFlex = 5;
const int kInvoiceQuantityColumnFlex = 2;
const int kInvoiceUnitPriceColumnFlex = 3;
const int kInvoiceTotalColumnFlex = 3;

/// Horizontal gap between adjacent columns, shared by the header and row
/// widgets so column edges stay aligned.
const double kInvoiceColumnGap = 6;
