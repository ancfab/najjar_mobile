// Mock signed-in user info shared by the Home header and bottom-tab
// navigation (Profile tab), so both entry points show the same name.
//
// auth/user API once it exists.
const String kCurrentUserName = 'Alex Sterling';
