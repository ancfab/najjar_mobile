<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all ANC API calls (login, etc.) in release builds.
         debug/AndroidManifest.xml and profile/AndroidManifest.xml already
         declare this for Flutter tooling (hot reload/VM service), which
         masks its absence here during `flutter run` — release builds only
         merge this main manifest, so without it every network call fails
         at the socket layer (SocketException: Permission denied). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Avatar upload: camera capture for "Take Photo". No storage
         permission is requested for "Choose from Gallery" — the gallery
         picker uses the system Photo Picker / document picker, which needs
         none. See lib/services/avatar_permission_service.dart and
         avatar_picker_service.dart. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+ (API 33) granular media permission, requested via
         permission_handler's Permission.photos before the gallery picker is
         launched. permission_handler reports this as already granted on
         older Android versions, where it isn't required. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Camera is used for avatar capture but isn't essential to the app,
         so devices without one (e.g. some tablets) can still install it. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:label="ANC Fabrics"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <!-- flutter_secure_storage (10.3.1) encrypts values with an
             Android Keystore-held key. Android Auto Backup can restore the
             plugin's encrypted-value store to a new/reset device without
             that non-exportable key, which the package's own README
             documents as a cause of InvalidKeyException; disallowing
             backup avoids restoring an undecryptable secure session and
             keeps the session envelope from being transferred off-device
             at all. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Required by image_cropper (12.2.1): its Android implementation
             launches com.yalantis.ucrop.UCropActivity via
             startActivityForResult, but the plugin's own AAR manifest does
             not declare that Activity — the consuming app must. Without
             this, Android can't resolve the component
             (ActivityTaskManager: START_CLASS_NOT_FOUND / result code -92)
             and the app crashes with "Reply already submitted" when
             image_cropper's ActivityResultListener later tries to complete
             the already-failed platform-channel call. Affects both the
             Camera and Gallery avatar-picker sources, since both route
             through AvatarCropperService.cropToSquare(). Theme comes from
             androidx.appcompat, already a transitive dependency of ucrop
             itself. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
